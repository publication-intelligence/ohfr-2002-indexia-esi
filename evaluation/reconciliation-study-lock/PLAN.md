# Benchmark reconciliation preparation — authority not selected

Status: preparation only; authority selection is pending candidate-blind source comparison among legacy v3, the native benchmark, or a reviewed v4. PR #3 remains draft. No registered evaluation artifact, frozen benchmark, audit, candidate visibility, or prior handoff is changed by this preparation.

## Verified incumbent-baseline identity and conditional reuse

The incumbent legacy v3 freeze is `98dbffd0ca171b5b7db76dbe1b2b5d5265ccacab`. The preserved import has the specified file SHA and canonical SHA; the read-only [verification](preparation-verification.json) records them. After the approved `relationships[*].type` to `relationship_type` rename, every non-wrapper top-level value is exactly equal between that legacy release and the current import. This covers subjects, evidence, priorities, relationships, reader tasks, exclusions, uncertainties, synthesis/review provenance, and source/page-map/chunk identities. The current public denominator remains 1,366 subjects and 3,210 expected treatments.

The differences are `benchmark_id`, `version`, `evaluation_id`, `policy_sha256`, `freeze`, and `benchmark_sha256`, plus the added `compatibility_import` provenance. This identity comparison does not establish that v3 is better than the native benchmark or select it as the study authority. Those are historical wrapper differences; this preparation does not replace or reinterpret their freeze as a fresh review.

All 113 current registered files match the final V8.2 private handoff byte-for-byte. All 17 locator audits retain the current candidate binding; all 17 missing-access audits retain the current candidate and benchmark bindings. Their frozen substantive judgments remain reusable for the semantically identical study benchmark. Candidate normalization, source mapping, chapter partition, and the complete structure judgments likewise require no new substantive review merely to declare this study lock. This is a reuse conclusion, not a claim that future wrapper hashes can remain unchanged.

## Conditional implementation only after authority selection and methodology support

1. Await the candidate-blind source comparison and final authority choice. Do not bind a study lock or migrate further before that decision. If v3 is selected, verify the reviewed methodology release and its exact study-lock and density-basis contracts. Do not add guessed schema fields or mutate registered state before those commands are available.
2. If v3 is selected, preserve the existing final V8.2 archive and receipts. Add the typed study-lock declaration using the exact legacy v3 identity and original release/review evidence. Retain the actual retrospective `candidate_seen=true`, original candidate-blind provenance, and historical compatibility approval.
3. If the new contract changes the imported benchmark wrapper or policy, rebuild only those wrappers and their self-hashes. Rebind the canonical candidate-to-benchmark pointer, dependent missing-access audit wrappers/ownership metadata, registered packets or inventory bindings where required, structure/input references, and state artifact hashes. Check each current schema; do not substitute hashes into historical review/approval evidence. Preserve all substantive arrays and stable IDs exactly.
4. Bind the approved common density measurement separately. The current 17 chapter measurements total **194,718 indexable source words**, citing legacy source-discovery artifact hashes. Candidate-specific locator and path counts remain candidate-specific. A study benchmark identity alone does not establish a common word-count basis. Wait for the approved chapter measurements, their provenance, and the typed density-basis contract; no alternative denominator has been inferred here.
5. Re-register affected structure/inputs and rebuild score/report/public bundles through supported registered commands. If only metadata changes, prove complete numeric and diagnostic invariance. If the approved density basis changes numerical denominators, calculate and explain the resulting density/component/overall changes explicitly; do not force the prior 71.30 score. Separate any newly observed cap consequences from metadata changes.
6. Validate all registered/private artifacts and the entire public projection. Compare every preserved judgment, treatment/task ownership, benchmark semantic field, and candidate-derived count; record an exact old/new hash ledger. Stage a new durable private handoff beside the existing archive. Keep PR #3 draft pending coordinator clearance; do not merge, activate, publish, or claim cross-evaluation comparability prematurely.

## Outstanding inputs

- Candidate-blind comparison and final choice among legacy v3, native, or reviewed v4. If another benchmark is selected, reassess substantive reuse rather than treating this conditional v3 plan as authorization.

- Reviewed methodology implementation and installed release receipt for the study-lock wrapper and density-basis binding.
- Authoritative common density chapter measurements and provenance (or an explicit decision that the existing basis is the shared basis).
- Coordinator confirmation that the other evaluation migrations satisfy the same study lock, policy, source scope, and density contract before any comparative claims.

Run `python3 evaluation/reconciliation-study-lock/verify-preparation.py` to reproduce this bounded inspection. This does not rerun discovery or audits and does not author new independent approval.
